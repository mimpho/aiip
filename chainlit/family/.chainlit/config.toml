[project]
# App familiar — perfil real del TFM
name = "AIIP — Perfil Familiar"

[UI]
# Nombre del asistente (obligatorio para Chainlit — UISettings.name)
name = "AIIP"
# Modo de tema por defecto
default_theme = "dark"
# Hoja de estilos con los tokens de diseño de E-02
custom_css = "/public/style.css"
# Punto de entrada único de custom_js: theme toggle + login heading +
# enlace "¿Olvidaste tu contraseña?" (D-040 punto 5) + indicador de
# "pensando" del chat. Chainlit solo admite un archivo custom_js, así
# que todo vive aquí (era auth.js, luego app.js, renombrado a custom.js
# el 16 jul 2026 al crecer más allá del login).
custom_js = "/public/custom.js"
# Chain of Thought (CoT) display mode — necesario para que cl.Step
# ("Documentos consultados", D-035/T-03) se muestre en la UI.
cot = "full"

[features]
# Deshabilitar telemetría de Chainlit
telemetry = false
# Evita que el scroll siga el streaming de la respuesta hasta el final;
# se queda arriba, en el inicio del mensaje, para que se pueda leer desde
# el principio (config real de la app familiar — CHAINLIT_APP_ROOT=chainlit/family,
# D-039; el .chainlit/config.toml de la raíz del repo no lo usa esta app).
assistant_message_autoscroll = false

[meta]
# Requerido por Chainlit — chainlit/config.py::load_settings() rechaza
# cualquier config.toml sin [meta].generated_by > "0.3.0" (D-038/T-05).
generated_by = "2.11.1"
